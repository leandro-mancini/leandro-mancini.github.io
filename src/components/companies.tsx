import { Container, Flex } from 'styled-system/jsx'
import { Text } from './ui'

export const Companies = () => {
    return (
        <Flex>
            <Container>
                <Text>Companies</Text>
            </Container>
        </Flex>
    );
}